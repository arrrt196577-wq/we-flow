/**
 * Repository ports declared by the core module. Implementations live in infrastructure;
 * core must not depend on MyBatis-Plus or persistence entities.
 */
package org.example.weflow.core.repository;
